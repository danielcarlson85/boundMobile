<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="Bound.NFC.MainPage"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Bound.Tablet.ViewModels;assembly=Bound.Tablet"
             x:Name="thisPage"
             Title="Plugin NFC Sample"
             BackgroundImageSource="Bound1.png">

    <ContentPage.BindingContext>
        <viewModels:MainPageViewModel />
    </ContentPage.BindingContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <Label FontSize="16" Grid.Row="2" 
           Text="{Binding MainPageTextLabel}" 
           TextColor="GreenYellow" 
           HorizontalTextAlignment="Center"/>
    </Grid>
</ContentPage>
